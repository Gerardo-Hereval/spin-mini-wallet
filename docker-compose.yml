services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      DATABASE_PATH: /data/wallet.db
    volumes:
      - db-data:/data

volumes:
  db-data:
